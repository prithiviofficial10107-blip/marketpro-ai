# Task List - Standardize Portal Address & Fix Connectivity

- [x] Research address usage in `START_NEW_PORTAL.bat`, `app.py`, and `.env`.
- [x] Standardize `START_NEW_PORTAL.bat` to use `localhost` and clean terminal output.
- [x] Update `app.py` startup message to use `localhost`.
- [x] Update frontend `.env` to use `localhost`.
- [x] Verify system starts and opens correctly at `http://localhost:5173`.
